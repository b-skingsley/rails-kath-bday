<div class="h-screen screen-height w-screen">
  <div id="carouselExampleControls" class="carousel bg-black slide h-2/3" data-ride="carousel">
    <div class="carousel-inner h-full">
      <% @posts.each_with_index do |post, i| %>
        <div class="carousel-item h-full w-full bg-center bg-contain bg-no-repeat <%= i == 0 ? 'active' : '' %>" style="background-image: linear-gradient(rgba(0,0,0,0.1),rgba(0,0,0,0.1)), url('<%= cl_image_path post.photo.key %>')"></div>
      <% end %>
    </div>
    <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
      <span class="carousel-control-prev-icon" aria-hidden="true"></span>
      <span class="sr-only">Previous</span>
    </a>
    <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
      <span class="carousel-control-next-icon" aria-hidden="true"></span>
      <span class="sr-only">Next</span>
    </a>
  </div>
  <div class="mx-10 mt-4 text-sm md:text-base text-center">
    <p>Welcome to Kath's 30th Birthday Website! This website is for everyone at Kath's party to post all the photos they take tonight with a message for Kath to save all of the memories from her 30th with all of you!</p>
    <%= link_to 'Click here to post a picture!', new_post_path, class: "mt-3 relative inline-flex items-center px-4 py-2 border border-transparent text-xs md:text-base md:text-sm font-medium rounded-md text-white bg-indigo-600 shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" %>
  </div>
</div>